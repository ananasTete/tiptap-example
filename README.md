# tiptap

1. StartKit 默认包含常见的 markdown 节点以及快捷输入语法

# 气泡菜单

1. 使用 @floating-ui/react 实现气泡菜单中的 select 功能，来解决碰撞检测等问题。

2. **统一展开方向**：在 `BubbleMenu` 父组件中统一计算屏幕空间（基于选区位置），将展开方向（向上或向下）作为 Prop 下发给所有子菜单，确保视觉整齐划一。
